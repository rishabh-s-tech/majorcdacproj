# Official Grafana image with our datasource + dashboard provisioning baked in, so it
# comes up pre-configured with no manual click-through, same as the local setup.
FROM grafana/grafana:11.4.0
COPY provisioning /etc/grafana/provisioning
